# Campaign creator in web3

## Getting Started

- cd into ether
- `npm install`
- `node deploy.js`
- ignore the message about `Invalid asm.js: Invalid member of stdlib`
- output will be sent to a newly-created build folder